#include<bits/stdc++.h>

typedef vector<int> vi;
typedef vector<vi> vii;
typedef long long ll;
using namespace std;


/*
la forma en la que almacenamos nuestros datos es esencial para optimizar 
el tiempo en que hacemos queries a estos. 



set: 
- ordenado, elementos unicos

- search, insert, delete logaritmico (pq todo esta sorteado)

set.count(element) retorna 1 si esta, 0 si no esta.

tambien soporta auto x: set
lower_bound(x) retorna el y que cumple que es mas grande que x.



unordered_set:
desordenado


map:
guarda en pares


segment tree:
- agarra un segmento de un arreglo y nos da informacion de ese segmento.
- todo en tiempo logaritmico
- nos permite cambiar el valor de un elemento de un lugar







*/





int main(){
	ios::sync_with_stdio(0); cin.tie(0);	


	return 0;
}
